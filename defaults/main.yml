---
# defaults file for ansible-prometheus.io

base_download_url: "https://github.com/prometheus/prometheus/releases/download" #v3.9.1/prometheus-3.9.1.linux-amd64.tar.gz

prometheus:
  user: "prometheus"
  directories:
    extraction: /usr/local/lib64
    data: /var/lib/prometheus
    logs: /var/log/prometheus
